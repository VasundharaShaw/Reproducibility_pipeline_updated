# Install pyenv
curl https://pyenv.run | bash

# Add pyenv to PATH for this session and future ones
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PYENV_ROOT/shims:$PATH"
eval "$(pyenv init -)"

# Verify
pyenv --version
